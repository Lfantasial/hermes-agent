# 2026-04-15 (수)

## Cron Health
- All cron jobs healthy (no errors)

## System Status
- Gateway restart ok (npm)
- **OpenClaw:** Update available (npm 2026.4.12, current: pnpm stable)
- **Skills:** All up to date

## Security Audit Report
**Audit Time:** 2026-04-15 03:00 KST
**Status:** Completed with known false positives filtered

### 🔴 Critical Findings (3)

1. **Open Group Policy with Elevated Tools Enabled**
   - Configuration: `channels.telegram.groupPolicy="open"` with tools.elevated enabled
   - Impact: Prompt injection in open groups can become high-impact incident
   - Fix: Set `groupPolicy="allowlist"` and keep elevated allowlists tight

2. **Open Group Policy with Runtime/Filesystem Tools Exposed**
   - Configuration: `channels.telegram.groupPolicy="open"`
   - Exposed Contexts:
     * `agents.defaults` (sandbox=off; runtime=[exec, process]; fs=[read, write, edit, apply_patch]; fs.workspaceOnly=false)
     * `agents.list.main` (same configuration)
   - Impact: Prompt injection can trigger command/file actions in these contexts
   - Fix: Use `tools.profile="messaging"` for open groups, set `tools.fs.workspaceOnly=true`, and use `agents.defaults.sandbox.mode="all"` to sandbox filesystem access

3. **Reverse Proxy Headers Not Trusted**
   - Configuration: `gateway.trustedProxies` is empty
   - Impact: If Control UI exposed via reverse proxy, configure trusted proxies
   - Fix: Set `gateway.trustedProxies` to proxy IPs or keep Control UI local-only

### 🟡 Warning Findings (4)

4. **Auto-Allow Skills Enabled for Exec Approvals**
   - Configuration: `defaults.autoAllowSkills` enabled
   - Impact: Widens host exec trust beyond explicit allowlists
   - Fix: Disable `autoAllowSkills` and keep manual allowlists tight

5. **Potential Multi-User Setup Detected**
   - Heuristic signals indicate gateway may be reachable by multiple users
   - Detection: `channels.telegram.groupPolicy="open"`
   - Current Trust Model: Personal assistant (single trusted operator)
   - Mismatch: Runtime/process tools exposed without full sandboxing in at least one context
   - Fix: If mutually untrusted users, split trust boundaries (separate gateways + credentials, ideally separate OS users/hosts)

6. **Extension Plugin Tools Reachable Under Permissive Policy**
   - Enabled Extension: claude-mem
   - Permissive Tool Policy Contexts: `default`, `agents.list.main`
   - Impact: Untrusted input processing without proper tool restrictions
   - Fix: Use restrictive profiles (`minimal`/`coding`) or explicit tool allowlists excluding plugin tools for agents handling untrusted input

### 📊 Attack Surface Summary
- **Groups:** open=1, allowlist=0
- **Tools elevated:** enabled
- **Webhooks:** disabled
- **Internal hooks:** enabled
- **Browser control:** enabled
- **Trust Model:** personal assistant (not hostile multi-tenant)

### ✅ Known False Positives Excluded
- tavily-search (2 files)
- claude-mem (5 files)
- writing-skills (1 file)

### 🔒 Recommendations
1. Set `channels.telegram.groupPolicy="allowlist"` and configure allowlists for trusted users
2. Use `tools.profile="messaging"` for open groups to limit exposed tools
3. Set `tools.fs.workspaceOnly=true` and `agents.defaults.sandbox.mode="all"` to sandbox filesystem access
4. Disable `defaults.autoAllowSkills` to enforce explicit trust grants only
5. Configure `gateway.trustedProxies` if using reverse proxy for Control UI
6. If multi-user environment required, separate gateways and credentials per trust boundary
7. Use restrictive agent profiles (`minimal`/`coding`) or explicit tool allowlists for untrusted input
8. Exclude plugin tools from agent profiles handling untrusted input

---
*Report generated at 2026-04-15 03:00 KST*
