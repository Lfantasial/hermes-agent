# 2026-03-03 (Tue, KST)

## Pre-compaction memory flush (07:19)

### Durable 운영 변경
- 신규 크론 추가 완료: `github-repo-daily-briefing`
  - Job ID: `0f2e06c5-425c-40b2-845b-135e47277fe1`
  - 스케줄: `0 9 * * *` (Asia/Seoul)
  - 전달: `telegram:5487758242` 고정 (`announce`)
  - 세션 타겟: `isolated`
- 브리핑 포맷 정책 고정:
  1) 최근 7일 생성 + 스타 기준 Top 5
  2) 누적 스타 상위 Top 5(archived 제외)
  3) 각 레포 설명(무엇/왜 중요한지)
  4) OpenClaw/Claude Code 적용 아이디어 3~5개
  5) 당일 실행 액션 3개
  - 한국어, 중복 제거, 길이 제한(간결 요약) 유지

### 검증/운영 관찰
- 신규 GitHub 브리핑 크론 수동 실행 테스트 성공, `deliveryStatus: delivered` 확인.
- 반복 패턴 재확인: CLI에서 `gateway timeout after 30000ms`가 발생해도, 백그라운드에서 cron run이 완료/전달되는 경우가 있음.
  - 운영 원칙: CLI 종료코드만으로 실패 판단하지 않고 `openclaw cron runs --id <jobId>`의 `status`/`deliveryStatus`로 최종 판정.

### 지속 리스크(추적 필요)
- 보안 감사 상시 경고 2건 유지:
  1) `gateway.trusted_proxies_missing`
  2) `plugins.tools_reachable_permissive_policy` (`claude-mem`)
- false positive 제외 정책은 유지됨:
  - `tavily-search`, `claude-mem`, `writing-skills` 지정 파일군

### 상태 메모
- 일일 업데이트 체크 맥락에서 OpenClaw 업데이트 가용 신호(채널 stable) 관측, skills는 최신.
- 세션 중 `openai-codex` OAuth token refresh 실패 이벤트가 반복 발생함(재인증 필요 징후).

## Pre-compaction memory flush 추가 (07:21)

### 정합성 업데이트
- `session_status` 재확인 시점 기준 런타임 버전이 **OpenClaw 2026.3.1**로 표시됨(기존 업데이트 가용 신호 이후 실제 반영 상태로 해석 가능).
- 직전 OAuth refresh 실패 징후와 달리, 같은 세션에서 상태 조회는 정상 응답됨(간헐 장애 가능성 유지, 추적 필요).

## Pre-compaction memory flush 추가 (07:22)

### Agent 설정 스냅샷(요청 응답 기준)
- 기본 모델(Default): `openai-codex/gpt-5.3-codex`
- Fallback: `zai/glm-4.7`, `zai/glm-4.7-flash`, `zai/glm-4.7-flashx`
- 인증: `openai-codex:default` OAuth 정상(약 10일 유효)
- 채널 상태: Telegram `ON/OK` (accounts `1/1`)
- 게이트웨이: loopback reachable, systemd `enabled/running`
- 보안 경고 2건(`trustedProxies`, permissive tool policy)은 동일하게 지속

## Pre-compaction memory flush 추가 (08:12)

### Durable 설정 변경
- 사용자 요청으로 fallback 모델을 Codex 계열로 정렬.
  - 실행: `openclaw models fallbacks clear` 후 `openclaw models fallbacks add openai-codex/gpt-5.3-codex`
  - 결과: Fallback 목록이 `openai-codex/gpt-5.3-codex` 1개로 변경됨.
- 현재 모델 정책 요약:
  - Default: `openai-codex/gpt-5.3-codex`
  - Fallback: `openai-codex/gpt-5.3-codex`

### 운영 메모(신규 경고 관찰)
- models 설정 반영 시 Doctor warning 확인:
  - `channels.telegram.groupPolicy=allowlist`인데 `groupAllowFrom/allowFrom` 비어 있어 그룹 메시지는 모두 드롭될 수 있음.
- 직접 DM 채널(현재 Sam)에는 영향이 제한적일 수 있으나, 향후 그룹 운영 계획이 있으면 allowlist 또는 정책값 정리 필요.

## Pre-compaction memory flush 추가 (08:43)

### 운영 상태 스냅샷(요청 응답 기준)
- `openclaw status` 재확인 결과 시스템은 정상 가동:
  - Gateway reachable(약 11ms), systemd enabled/running
  - Telegram `ON/OK` (accounts 1/1)
  - 보안 요약 `0 critical / 2 warn / 1 info` 유지
- 모델 설정 재검증:
  - Default/Fallback 모두 `openai-codex/gpt-5.3-codex`로 유지
- 버전 관찰 포인트:
  - CLI는 2026.3.1, `openclaw status`의 Gateway app 표시는 2026.2.26으로 노출(동작상 문제는 없으나 표시 정합성 추적 필요)

## Pre-compaction memory flush 추가 (19:32)

### GPU 이벤트 로그 점검 결과(사용자 요청)
- 점검 범위: **어제(2026-03-02)부터 현재까지** 그래픽카드 관련 이벤트 로그.
- WSL/Linux 측(`journalctl`, `dmesg`)에서는 의미 있는 GPU 오류는 확인되지 않았고, `drm/vgem initialized` 수준의 정보성 로그만 관측.
- Windows System Event Log 조회 결과(WSL에서 PowerShell 실행):
  - GPU/그래픽 관련 이벤트 총 **59건**
  - `nvlddmkm` 오류 **50건** (모두 Error 레벨)
    - Event ID `153`: 48건
    - Event ID `14`: 2건
    - 발생 구간: **2026-03-02 15:13:18 ~ 23:30:31**
  - `Parsec Virtual Display Driver` 이벤트 **6건**(attach/detach 계열), 최신 2026-03-03 05:08:32
- 결론: **3/2 오후~야간에 NVIDIA 드라이버 계열 오류가 집중 발생**한 이력 존재.

### 운영 교훈
- WSL 환경에서 Windows 이벤트 로그 조회 시 `powershell.exe`가 PATH에 없을 수 있어, `/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe` 전체 경로 호출이 안정적.